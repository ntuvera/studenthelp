<h1>{{title}}</h1>

{{#each category}}
	{{#each this.Posts}}
	<div class="post">
		<div class="voting">
			<span class="score" data-post="{{this.id}}">{{this.score}}</span>
			<button class="btn-xs btn-success up fa fa-heart"></button>
		</div>
		{{#if this.image}}
			<img class="img" src="{{this.image}}"/>
		{{else}}
			<img class="img" src="/image/bootuplogo.png"/>
		{{/if}}

		{{#if this.url}}
			<a href="{{this.url}}">{{this.title}}</a>
		{{else}}
			{{this.title}}
		{{/if}}	
		<a href="/posts/{{this.id}}" class="btn btn-primary pull-right">View Details</a>
	</div>
{{/each}}
{{/each}}